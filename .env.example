# All credentials live in ~/.env (master env file).
# Services that need them either:
#   - source ~/.env directly (mc script)
#   - use EnvironmentFile=~/.env (systemd services)
#   - have their own .env copy (immich - docker compose requires local .env)
#
# See ~/.env for the full list of credentials.
